version: "3"

services:
  nginx:
    image: nginx
    volumes:
      - ./nginx/conf:/etc/nginx/conf.d/default.conf
      - /var/www/html:/var/www/html
      - /etc/letsencrypt:/etc/letsencrypt
    ports:
      - 443:443
